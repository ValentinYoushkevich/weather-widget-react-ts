import WeatherWidget from "@/components/weather/WeatherWidget";

const Index = () => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-[hsl(220,15%,20%)]">
      <WeatherWidget />
    </div>
  );
};

export default Index;
